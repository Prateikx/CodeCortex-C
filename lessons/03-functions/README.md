# Functions

Goal: learn function declarations, parameters, return values, scope, and recursion.

## Exercises

1. Write `add(int a, int b)` and return the sum.
2. Write `is_prime(int n)` without using global state.
3. Write a recursive factorial function and handle invalid input.
4. Explain in comments why passing a pointer allows a function to modify the caller's variable.
